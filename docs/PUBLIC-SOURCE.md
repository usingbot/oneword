# Public source publication — 2026-09-19

Scope: publish Git source to https://github.com/usingbot/oneword only.
No version tag, GitHub Release, production bundle, deployment or Liberation
artifact upload is authorized by this step. Liberation remains
**LIKELY CLOSED — NEEDS LEGAL REVIEW**. No legal approval is claimed.

## Pre-push inspection

Started on clean main at 4e3a5df7917c1894204597b1ea3193f3bee4d41b, with no remote.
Reviewed all 149 tracked paths and scanned 304 historical blobs reachable from
main (12 commits), so first-push history was included in the check.

- No tracked/current or historical Liberation TTFs, font binaries, dist,
  node_modules, source/release archives, PDFs, backups, caches or private artifacts.
- The only binary file paths in history are the three original OneWord PNG
  icons, generated by scripts/generate-icons.mjs without fonts/third-party art.
- PDF and backup tests generate synthetic content from source fixtures.
- Secret-pattern findings were credential-bearing URL rejection fixtures using
  reserved .test domains in three historical versions of study-pack.test.ts.
  They are not real credentials. No real secret/private artifact was detected;
  pattern scanning is not proof that every possible secret is absent.
- Font downloads/build output exist only outside tracked Git content. No font
  binary or archive is transmitted by the explicit main-only Git push.

Local ignored scan evidence is artifacts/public-source-scan.json.

## Initial source publication evidence

Configured origin to https://github.com/usingbot/oneword.git and pushed only
main:refs/heads/main, with followTags=false, no force and no history rewrite.
git ls-remote confirmed public main equaled
4e3a5df7917c1894204597b1ea3193f3bee4d41b. An unauthenticated GitHub API request
returned that same commit with HTTP 200; the repository API reports private=false.

The actual hosted [Core quality run](https://github.com/usingbot/oneword/actions/runs/35422489892)
for that exact initial commit completed successfully. This is hosted GitHub Actions
evidence, not the earlier local Linux simulation.

## Source mapping follow-up

The follow-up changes only release tooling/metadata/tests and explanatory docs.
applicationSource.mode=public-head delegates exact identity to live checks in
scripts/public-source.mjs, avoiding a tracked file that must contain its own SHA.
The final follow-up commit and its hosted CI result are reported after its push;
do not mistake the initial SHA above for an immutable final release designation.

The command requires clean local Git, matching public main/HEAD and commit/tree
objects, a public source tree and unauthenticated matching README, LICENSE,
SECURITY.md and CONTRIBUTING.md. It prints the exact checked source URL each time.
Seven added regression cases cover successful dynamic mapping and rejection of a
dirty tree, stale public main, private repository, changed tree, changed document
and unavailable public request. All 19 release-validation tests passed locally.

The existing Liberation check is unchanged. A successful source-access check
removes only source identity/access failures; release:check must still reject
the unresolved Liberation review/distribution arrangement. No independent legal
review or maintainer risk acceptance is recorded as complete.

README and source guides now distinguish public source availability from an
approved binary release. Contacts remain owner-verified and version remains 0.1.0.
Application runtime, build behavior, dependencies and browser tests are unchanged.
No full browser rerun is claimed for this source-publication tooling change.
